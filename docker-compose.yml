version: '3.8'

services:
  # Application React optimisée pour Hostinger
  app:
    build: .
    container_name: facture-app
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
